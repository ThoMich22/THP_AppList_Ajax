<li >
<%= task.title  %>
<% unless task.deadline.nil? %>
, to read: <%= task.deadline.strftime('%D')  %>
<% end %>
    <p>
    <i class="fas fa-trash"> <%= link_to "delete", task_path(task), method: "delete", remote: true%></i>
    </p>  
</li>

